let value;

// Number to String
value = String(10);

// Boolean to String
value = String(false);

// Array to String
value = String([1, 2, 3]);

// Object to String
value = String({ name: "Dorian" });

console.log(value);
console.log(typeof value);
