/**
 * Сортировка списка
 * Ввод: список чисел
 * Вывод: отсортированный список по возрастанию
 */
const array = [2, 4, 1, 7, 92, 5, -2, 88, -10];
const sortedArray = array.sort();
alert(sortedArray);
